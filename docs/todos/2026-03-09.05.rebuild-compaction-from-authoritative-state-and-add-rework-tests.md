# TODO: Rebuild compaction from authoritative state and add rework tests

## Current Status
- State: Not Yet Started
- Dropped Reason: 
- Last Status Update: 2026-03-09 09:28:04

## Context
This slice covers the remaining consistency and verification work after the stable-prefix inputs, prompt assembly, and execution model are implemented. The finalized design requires compaction to rebuild the stable prefix from authoritative runtime/session state rather than relying on model-produced summaries or stale mutable prompt sections.

It also requires broad test coverage for prompt stability, transcript ordering, execution batching, detached completions, and delta handling.

## Why It Is Needed
Without this slice, the new design could drift in long sessions or regress silently. Compaction is part of the core contract now because the stable prefix must remain authoritative after history compression. The redesign also needs a stronger test suite to protect prompt-shape and execution semantics once implementation lands.

## Completion Criteria
- [ ] Rework compaction to rebuild the stable prefix from authoritative runtime/session state.
- [ ] Fold superseded contract deltas into rebuilt stable-prefix state during compaction.
- [ ] Ensure summarized history preserves the append-only event-log model after compaction.
- [ ] Add prompt-shape tests covering stable-prefix reuse and event ordering.
- [ ] Add execution tests covering batch settle, detached completions, and event emission behavior.
- [ ] Update architecture and prompt-design docs to match the implemented rework.

## Detailed Plan
1. Revisit session compaction around the new stable-prefix model.
2. Rebuild prefix material from authoritative runtime/session state during compaction.
3. Preserve only the necessary summarized events after baseline rebuild.
4. Add prompt and execution regression tests covering the reworked harness.
5. Refresh docs to reflect the final implemented behavior.

## Activity Log
- [2026-03-09 09:28:04] [USER] User asked to derive implementation TODOs from the finalized prompt/context design and begin execution.
- [2026-03-09 09:28:04] [ACTION] Created the implementation tracker for compaction, verification, and documentation follow-up after the core rework lands.
- [2026-03-09 09:28:04] [RESULT] The remaining compaction and test work is now tracked as a dedicated implementation slice under docs/todos.
