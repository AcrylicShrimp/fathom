# TODO: Rebuild compaction from authoritative state and add rework tests

## Current Status
- State: Finished
- Dropped Reason: 
- Last Status Update: 2026-03-09 11:57:24

## Context
This slice covers the remaining consistency and verification work after the stable-prefix inputs, prompt assembly, and execution model are implemented. The finalized design requires compaction to rebuild the stable prefix from authoritative runtime/session state rather than relying on model-produced summaries or stale mutable prompt sections.

It also requires broad test coverage for prompt stability, transcript ordering, execution batching, detached completions, and delta handling.

## Why It Is Needed
Without this slice, the new design could drift in long sessions or regress silently. Compaction is part of the core contract now because the stable prefix must remain authoritative after history compression. The redesign also needs a stronger test suite to protect prompt-shape and execution semantics once implementation lands.

## Completion Criteria
- [x] Rework compaction to rebuild the stable prefix from authoritative runtime/session state.
- [x] Fold superseded contract deltas into rebuilt stable-prefix state during compaction.
- [x] Ensure summarized history preserves the append-only event-log model after compaction.
- [x] Add prompt-shape tests covering stable-prefix reuse and event ordering.
- [x] Add execution tests covering batch settle, detached completions, and event emission behavior.
- [x] Update architecture and prompt-design docs to match the implemented rework.

## Detailed Plan
1. Revisit session compaction around the new stable-prefix model.
2. Rebuild prefix material from authoritative runtime/session state during compaction.
3. Preserve only the necessary summarized events after baseline rebuild.
4. Add prompt and execution regression tests covering the reworked harness.
5. Refresh docs to reflect the final implemented behavior.

## Activity Log
- [2026-03-09 09:28:04] [USER] User asked to derive implementation TODOs from the finalized prompt/context design and begin execution.
- [2026-03-09 09:28:04] [ACTION] Created the implementation tracker for compaction, verification, and documentation follow-up after the core rework lands.
- [2026-03-09 09:28:04] [RESULT] The remaining compaction and test work is now tracked as a dedicated implementation slice under docs/todos.
- [2026-03-09 11:52:51] [USER] User asked to proceed with the next todo items after finishing the execution-batch slice.
- [2026-03-09 11:52:51] [ACTION] Started the final rework slice by inspecting the remaining compaction path and the broader regression coverage gaps around prompt ordering and execution events.
- [2026-03-09 11:52:51] [RESULT] The compaction-and-tests tracker is now active and focused on authoritative baseline rebuilds plus redesign regression coverage.
- [2026-03-09 11:57:24] [USER] User asked to proceed with the next todo items after the execution-model slice, which advanced the final compaction-and-tests work.
- [2026-03-09 11:57:24] [ACTION] Aligned compaction summaries with the new transcript model, added authoritative-baseline rebuild coverage, added prompt ordering and execution event-emission regressions, and refreshed the prompt-design doc to reflect the implemented invariant.
- [2026-03-09 11:57:24] [RESULT] The final rework slice is finished; compaction now preserves the append-only event model while stable prefix snapshots are verified to rebuild from authoritative session/runtime state.
